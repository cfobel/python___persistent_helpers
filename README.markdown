# persistent_helpers.PersistentOrderedDict #

This class implements the same interface as the `collections.OrderedDict`
class from the standard library, but uses `persistent` data types for ZODB
support.
